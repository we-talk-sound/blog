
const initialState = {};

const userReducer = (state = initialState, action: { [key: string]: any }) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;

